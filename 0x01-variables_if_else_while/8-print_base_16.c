#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int p;

	char z;

	p = 0;
	
	z = 'a';

	for (p = 0; p <= 0; p++)
		putchar(p);
	for (z = 'a'; z <= 'f'; z++)
		putchar(z);
	putchar('\n');
	return (0);
}

